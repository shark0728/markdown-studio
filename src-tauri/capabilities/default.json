{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Permissions for the Markdown Studio main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "store:default"
  ]
}
