# 更新日志

## 0.2.1 - 2026-09-07

### 修复

- 加粗、斜体和删除线选区首尾含空白时预览不生效；保留外侧空白，跨空行时按段落格式化。
- 行内代码中包含反引号或两端空格时预览内容错误；使用匹配的代码分隔符与必要的空格，并通过 AST 识别可撤销的原有代码范围。
- 大量反引号选区生成代码块时因函数参数过多而抛出异常。
- 打开或重新打开文档后预览继承旧文档滚动位置；普通编辑、保存和视图切换保持当前阅读位置。
- 初始化偏好读取尚未完成时，原生拖拽等更新以默认设置覆盖原偏好；启动更新现在排队合并，关闭前等待加载与写入完成。

### 验证

- 新增 Markdown 解析结果、格式撤销、启动时序和预览滚动回归用例。
- 扩展真实 CodeMirror 格式边界测试，以及原生双文件切换、CRLF 保存和关闭烟测。

## 0.2.0 - 2026-09-07

### 新增

- Markdown 格式工具栏、选区格式切换、多光标操作与撤销/重做。
- 最近文件面板：筛选、打开、移除与清空记录。
- 仅编辑/左右分栏/仅预览模式及持久化。
- 搜索替换入口、自动换行/行号设置、行列与选区统计。
- 分栏的键盘调整与双击复位。
- 真实 CodeMirror 的浏览器端到端回归测试与多尺寸截图验证。

### 修复

- 保存期间继续编辑时，未写入磁盘的修改被错误标记为已保存。
- 文件选择器异常未提示、并发文件操作可能交错。
- 桌面视图切换无效、窄窗口编辑区仍保持半宽。
- 最近文件只有存储没有入口，搜索按钮仅聚焦不打开搜索面板。
- 未保存确认对话框缺少焦点限制和后台快捷键隔离。
- Setext 标题、嵌套标题、长代码围栏和预览锚点不一致。

### 边界

- 保持单文档、本地优先，不引入自动保存、正文缓存、云同步或多标签。
- 外部修改冲突检测、事务式磁盘替换、本地图片资源管理和超大文档性能仍待后续版本。
