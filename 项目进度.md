# 项目进度

更新时间：2026-09-02

## 当前任务目标

从空目录实现一个 Windows 本地优先 Markdown 查看编辑器 v1。

## 已完成

- 已确认目录初始为空。
- 已确定并创建 Tauri 2 + React + TypeScript + Vite 项目骨架。
- 已接入 Tailwind、shadcn/ui 风格按钮、CodeMirror 6、GFM 实时预览和安全 HTML 过滤。
- 已实现单文档状态、打开/新建/保存/另存为、拖拽打开、未保存提示和关闭拦截；原生读写命令仅允许 `.md`/`.markdown`。
- 已实现标题大纲、编辑器/预览响应式切换、主题/字号/分栏比例/最近文件持久化。
- 已生成 Windows 应用图标与 MSI/NSIS 打包配置。
- 已初始化本地 Git 仓库，默认分支为 `main`，并通过 `.gitignore` 排除依赖、构建目录和本地状态。
- 已创建首个源码备份提交：`43519d49d1581920b6ed8fbdbd210f8c6bace89c`。
- 已按用户要求删除外部 Git Bundle 备份 `MD-git-backup-20260831-0fbbd4f.bundle`。
- 已按用户要求删除恢复副本 `MD-git-restored-backup-0fbbd4f`；源码仓库仍保留。
- 已创建公开 GitHub 仓库 `https://github.com/shark0728/markdown-studio`，并将本地 `main` 推送到 `origin/main`。
- 已完善 GitHub 项目信息：新增中英双语 `README.md`，设置项目描述和 13 个 Topics；未修改仓库可见性、Issues、Projects 或 Wiki 开关。
- 已加入 MIT 许可证 `LICENSE`，版权归属为 `shark0728`（2026）。
- 已创建 GitHub Release `v0.1.0`，上传 MSI、NSIS 和独立 EXE 三个 Windows x64 发布包。
- 已用 GPT image 2 生成新版图标，并替换 Tauri 活跃图标资源；旧 SVG 图标已从活跃资源中移除，版本升级为 `0.1.1`。
- 已重新生成 Windows/Android/iOS 图标资源，并发布 `v0.1.1`。
- 已修复大纲跳转、编辑区滚动和窗口关闭问题，版本升级为 `0.1.2`，并完成 GitHub Release 发布。

## 进行中

- 无。

## 待办

- 后续可补充复杂本地图片路径和超大文档性能优化。

## 已知问题

- Vite 对约 1 MB 的主 JavaScript chunk 给出体积提示，不影响当前构建和运行，后续可按需拆包。
- 系统文件选择器和关闭确认对话框未逐项做 UI 自动化；文件成功/失败状态已由单元与集成测试覆盖。

## 验证结果

- Node.js v24.18.0、npm v11.16.0、Rust/cargo v1.97.1 已检测。
- `npm run build`：通过。
- `npm run lint`：通过，零警告。
- `npm audit`：通过，0 个已知依赖漏洞。
- `npm run test`：通过，5 个测试文件、13 个测试用例。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过。
- `cargo test --manifest-path src-tauri/Cargo.toml`：通过，2 个测试用例、0 个失败。
- `npm run tauri info`：通过，WebView2、MSVC、Rust 工具链均可用。
- `npm run tauri build`：通过，生成 MSI 和 NSIS 安装包。
- 发布可执行文件 SHA-256：`7D1A75F9CF40BAEBC9C7F25277F1206CFC35A80E830C966236737E78BB783796`。
- MSI 安装包 SHA-256：`BCB19511471FADDC3A97EBCC119099AACC8C0F8694B88E7F92E72A318DCDE6E0`。
- NSIS 安装包 SHA-256：`474EA7446F253F1B5FB9166C2686D05AE33609CDF1E6A7CBEE2332064B20F091`。
- 发布版 EXE 启动烟测：通过，进程已启动并正常结束。
- Windows 界面手工验收：通过；实际输入标题、GFM 任务列表和表格后，源码、大纲、实时预览、行数/字符数及未保存状态同步更新。
- 浅色/深色主题切换：通过；验收结束后已恢复浅色默认偏好。
- Git 提交前检查：通过；暂存 100 个文件，无超过 1 MB 的单文件，未命中常见密钥、密码或私钥模式。
- `git diff --cached --check`：通过。
- 备份删除后核验：Bundle 与恢复副本均不存在，项目 `.git` 目录仍存在。
- GitHub 上传核验：远程仓库存在且为公开仓库，`origin/main` 与本地 HEAD 一致。
- GitHub 信息核验：描述、Topics、README 和公开状态均已通过 GitHub API 读取确认。
- GitHub Release 核验：`https://github.com/shark0728/markdown-studio/releases/tag/v0.1.0` 可访问，三个资产均已上传。
- v0.1.0 发布包 SHA-256：EXE `698A889ED9BC1361AB9E97FE5B2B42FCD3F030629F5ADA9BDCA9D362D9152E5F`；MSI `F9088F6C74ED2B3FFB4667443F1B91EEFDBE14D8E1B7A7F6FE1D73D51271AC1C`；NSIS `0AE5DE6857DAA85B2ABBCE42B7C05CE7F56D3ADF7121CD6116D640F14CB59462`。
- 新版图标候选验证：PNG，1254×1254，1,431,266 字节，SHA-256 `6E138EED767E1411D2D7F6F9026E00EEC55AFA597690EC1891FFCD34C3DFFF41`。
- v0.1.1 活跃图标资源验证：`icon.ico` SHA-256 `276260E08AFE201ACA461241C68CC0E76B53D83CE85435D3173878DFA10A995F`；`icon.png` SHA-256 `D2E57FFCF0D81FD1C3E31308B20FA12949A3E48A4073685F3C43FF1FA5C18868`；`icon.icns` SHA-256 `1E3428FEE4924A0DE4C8D79775006D4927BCCDD8088F363B6A68479B2B6ED8C`。
- v0.1.1 `npm run lint`：通过；`npm run test`：13/13 通过；`npm run build`：通过；`cargo test`：2/2 通过；`npm run tauri build`：通过。
- v0.1.1 GitHub Release：`https://github.com/shark0728/markdown-studio/releases/tag/v0.1.1`；EXE SHA-256 `BDE15C51041FDBD7F9C19AEE830566F409281F53CA06439BA6EA67859927079F`；MSI `67309F73358CE0FDCB5D0801F0C6A8C877024940A64207A9BCE4D748964E089D`；NSIS `07C26A49C2BBDE9E69A4C85A9DA47F54674AF359B4BAF4825F30B9E291BFD8E0`。
- 修复版手工验收：编辑区滚轮可上下滚动，大纲点击可同步跳转源码与预览，干净窗口关闭按钮可正常退出。
- 修复版自动化测试：6 个测试文件、15 个测试用例通过。
- v0.1.2 GitHub Release：`https://github.com/shark0728/markdown-studio/releases/tag/v0.1.2`；EXE SHA-256 `0FEA01BA8B9BCDD70B380F4E10AE8943002C6EA808943933763E88D3AE94E02B`；MSI `078AEC3CCAA56BD51ADF4FCD64A1ED93DA693C6B43D89EA30F9393CFE40B53F4`；NSIS `642963FC566F44E41BE0A7047BA84B69757A0E0564882CA8ABE5E3B5F89745E9`。
